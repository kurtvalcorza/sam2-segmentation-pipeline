# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/sam2-segmentation-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/sam2-segmentation-pipeline/blob/main/tutorials/sam2_segmentation_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-facebook%2Fsam2.1--hiera--small-ffcc4d?style=flat)](https://huggingface.co/facebook/sam2.1-hiera-small)
[![Upstream](https://img.shields.io/badge/Upstream-facebookresearch%2Fsam2-181717?style=flat&logo=github&logoColor=white)](https://github.com/facebookresearch/sam2)
[![arXiv](https://img.shields.io/badge/arXiv-2408.00714-b31b1b.svg)](https://arxiv.org/abs/2408.00714)

Notebook specification: **DIMER Notebook Specification 1.1** — the notebook is **standalone** (§3.6): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's
pipeline module, model identity, snapshot manifest and runtime pins, so the exported `.ipynb` keeps working
without this repository being reachable. Do not edit the notebook by hand; edit the package or the template
and regenerate (`python tools/build_notebook.py`; `--check` is enforced by the validator and CI).

| Notebook | Profile | Carrier | Capability | Default runtime | BYOD | Release status |
|---|---|---|---|---|---|---|
| `sam2_segmentation_colab.ipynb` | `TASK-INFERENCE` | standalone (generated) | promptable image segmentation (one object per call from point and/or box prompts) with `facebook/sam2.1-hiera-small`; `(K, H, W)` boolean masks plus model-predicted, uncalibrated `iou_scores` (unclipped, may exceed 1.0); image mode only, video tracking out of scope; `validate_inputs` → input manifest; `evaluation_report` → `sample-sanity` with `mask_iou` per candidate only when a reference mask is supplied, otherwise `not-measurable`; no mIoU | CPU (CUDA used automatically when available) | single image file (16–4096 px per side) plus a click set through `POINT_X`/`POINT_Y`, gated off by default; no reference mask, so the report is `not-measurable` | **Candidate** — static checks pass; the clean-runtime execution run is pending and will be recorded in `../docs/release-verification.md`, which must be reviewed for the exact notebook revision before promotion |

## Conformance notes

- **Standalone carrier (ST1–ST6):** the primary path performs no clone, repository install, or repository import. Section 2 is `src/sam2_segmentation_pipeline/pipeline.py` verbatim (tagged `metadata.dimer.embedded_module`; the only rewrite makes the default weights directory working-directory-relative), Section 3 carries the model identity and the 7-file SHA-256 manifest inline and asserts they agree with the module before anything is fetched, and Section 1 carries the exact `pyproject.toml` runtime pins. `metadata.dimer.generated_from` records the repository, the revision it was generated from, the module SHA-256, and the generator version.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried module, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Model acquisition:** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries that are absent, from the Hugging Face Hub at the immutable revision (never `main`); `verify_snapshot` re-hashes every entry; `from_pretrained(weights_dir=WEIGHTS_DIR)` loads the verified files with `local_files_only=True` and `trust_remote_code=False`. The expected `sam2_video → sam2` load notice is explained in the opening cell and the prerequisites. The notebook's only external dependencies are the pinned PyPI distributions and the Hub at that revision.
- **Role stages:** `validate_inputs` (DAT24) writes `outputs/sam2_segmentation_input_manifest.json` with the schema, ceilings, the input's observed mode and size, how many clicks and whether a box was given, the cleaned prompt values, the verdict, and one recorded rejection finding from the click-outside-image probe; `evaluation_report` (EVAL21) writes `outputs/sam2_segmentation_evaluation_report.json` — `sample-sanity` with one `mask_iou` entry per candidate (marking the one the decision rule selected) when a reference mask is supplied, `not-measurable` on BYOD (with what labelled data would make the task measurable).
- The default sample is a synthetic 320×240 scene drawn in code (dark rectangle at [40, 60, 140, 180], red disc at [200, 80, 280, 160]) with one foreground click at (90, 120) — the same scene and click the card-pass smoke used; `mask_iou` against a rectangle you drew yourself is sanity evidence for the prompt, forward pass and up-sampling, not a segmentation metric (mIoU needs labelled masks and is not computed).
- Score semantics: `iou_scores` are the model's own uncalibrated predictions, not measured overlaps or probabilities, and the regression head is unclipped so a value may exceed 1.0; the conventional argmax candidate is used and stated; the pipeline ships no acceptance threshold and the caller owns any decision rule. The notebook performs no validation of its own — image, prompts and `multimask` are checked only by the pipeline's `validate_inputs`/`segment` path.
- Ceilings `MIN_IMAGE_SIDE` (16), `MAX_IMAGE_SIDE` (4096), `MAX_PROMPTS` (16), `NUM_MULTIMASK_OUTPUTS` (3) and `MASK_THRESHOLD` (0.0) are printed before the model runs; one object per call is stated; video tracking, automatic segmentation, text prompts and class labels are named as out of scope.
- CPU is documented as adequate (card-measured 4.8 s load, ~0.9 s per `segment` on 320×240; encoder cost fixed by the 1024×1024 resize).
- `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the
  clean-runtime execution requirement; a release review must confirm that a recorded clean run in
  `docs/release-verification.md` matches the notebook revision under review before the status is
  promoted to `Release-grade`.
